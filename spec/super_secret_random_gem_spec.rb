require "spec_helper"

describe SuperSecretRandomGem do
  it "has a version number" do
    expect(SuperSecretRandomGem::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
